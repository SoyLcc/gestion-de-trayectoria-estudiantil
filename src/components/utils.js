import styled from 'styled-components';

// Here will be the components that can be used in every page/component.

const Content = styled.div`
    width: 100%;
    padding: 20px;  
    background-color: #e4f6f3;
`;

export default Content;